### face_detection
This code reads the images from the Image_sources, then face recognition is done on them, and after that, the images that have faces are moved to the face folder, and the images that do not have faces are moved to the folder without faces.
